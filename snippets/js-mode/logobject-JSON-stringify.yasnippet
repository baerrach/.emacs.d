# -*- coding: utf-8; mode: snippet -*-
# name: wrap variable in string template to log its JSON.stringify value `var=${JSON.stringify(var)}`
# expand-env : ((yas-wrap-around-region nil))
# --
${1:`(if (region-active-p) (yas-selected-text) "var")`}=\${JSON.stringify($1)}$0