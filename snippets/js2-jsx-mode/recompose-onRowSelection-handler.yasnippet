# -*- mode: snippet -*-
# name: Recompose onRowSelection handler
# key: RE
# --
const onRowSelection${1:uiTableName}Handler = ({
  do${2:actionName},
}) => (selectedRows) => {
  if (selectedRows.length === 0) {
    setSelectedRowNumber(-1);
  } else {
    const row = selectedRows[0];
    setSelectedRowNumber(row);
    do$2();
  }
};
$0
