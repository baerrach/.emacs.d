# -*- mode: snippet -*-
# name: Recompose onKeyPress handler
# key: RE
# --
const onKeyPress${1:uiComponentName}Handler = ({
  do${2:ActionName},
}) => (event) => {
  if (event.charCode === 13) { // enter key pressed
    event.preventDefault();
    do$2();
  }
};
$0
