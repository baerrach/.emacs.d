# -*- mode: snippet -*-
# name: Recomponse onUpdateInput AutoComplete handler
# key: RE
# --
const onUpdateInputAutoComplete${1:uiComponentName}Handler = ({
  set${2:NewDataField},
}) => (
  searchText
) => {
  set$2(searchText);
};
$0